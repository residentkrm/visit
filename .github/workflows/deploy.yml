name: Deploy to Remote Server (Parallel Install)

on:
  push:
    branches:
      - main

jobs:
  # 🧩 1. Подготовка — git pull + очистка
  prepare:
    runs-on: ubuntu-latest
    steps:
      - name: Git pull & clean
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd /home/laradev.tech
            git pull
            rm -rf node_modules

  # ⚙️ 2. Установка зависимостей (часть 1)
  install:
    runs-on: ubuntu-latest
    needs: prepare
    steps:
      - name: NPM install
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            export NVM_DIR="$HOME/.nvm"
            [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
            cd /home/laradev.tech
            npm install

  # 🧪 3. Установка зависимостей (часть 2 — эксперимент)
  install-echo:
    runs-on: ubuntu-latest
    needs: prepare
    steps:
      - name: Echo hello world
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd /home/laradev.tech
            echo "Hello World — install phase 2 running in parallel"

  # 🚀 4. Сборка и рестарт приложения
  build-and-restart:
    runs-on: ubuntu-latest
    needs: 
      - install
      - install-echo
    steps:
      - name: Build & restart app
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            export NVM_DIR="$HOME/.nvm"
            [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
            cd /home/laradev.tech
            npm run build
            sudo service supervisor restart
